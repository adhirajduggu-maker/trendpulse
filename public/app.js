let S={trends:[],geo:"IN",stage:"all",comp:"",search:""};const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],E=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function filt(){return S.trends.filter(t=>(S.stage==="all"||t.stage===S.stage)&&(!S.comp||t.competition===S.comp)&&(!S.search||t.title.toLowerCase().includes(S.search)))}
function render(){let a=filt();$("#rows").innerHTML=a.map(t=>`<div class="row" data-id="${t.id}"><div class="trend-name"><b>${E(t.title)}</b><small>${E(t.niche)} • ${E(t.platform)} • window ${E(t.window)}</small></div><div class="traffic">${E(t.stage)}</div><div class="growth">${E(t.trafficText)}</div><div><span class="comp ${t.competition}">${t.competition}</span></div><div class="score" style="--s:${t.score}"><span>${t.score}</span></div></div>`).join("")||'<div class="loading">No matches.</div>';$$(".row").forEach(x=>x.onclick=()=>report(S.trends.find(t=>t.id===x.dataset.id)))}
async function load(){try{let r=await fetch(`/api/trends?geo=${S.geo}`),d=await r.json();if(!r.ok)throw Error(d.error);S.trends=d.trends;$("#hot").textContent=d.stats.hot;$("#signals").textContent=d.stats.signals;$("#low").textContent=d.stats.lowCompetition;$("#exploding").textContent=d.stats.exploding;$("#updated").textContent=`Updated ${new Date(d.fetchedAt).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})} • ${d.geo}`;render();today()}catch(e){$("#rows").innerHTML=`<div class="loading">${E(e.message)}</div>`}}
async function today(){let r=await fetch(`/api/post-today?geo=${S.geo}&niche=${encodeURIComponent($("#niche").value)}&platform=${encodeURIComponent($("#platform").value)}`),d=await r.json();$("#today").innerHTML=d.opportunities.map((t,i)=>`<article class="today-item" data-id="${t.id}"><span class="rank">#${i+1} OPPORTUNITY</span><h3>${E(t.title)}</h3><span class="pill good">${E(t.stage)}</span><span class="pill">${E(t.competition)} comp.</span><span class="pill">${E(t.niche)}</span><p>${E(t.hooks[0])}</p><div class="today-score">Fit ${t.fitScore}/100 →</div></article>`).join("");$$(".today-item").forEach(x=>x.onclick=()=>report(d.opportunities.find(t=>t.id===x.dataset.id)))}
function report(t){$("#report").innerHTML=`<span class="eyebrow">OPPORTUNITY REPORT</span><h2>${E(t.title)}</h2><p>${E(t.niche)} • ${E(t.platform)}</p><div class="report-grid"><div class="report-stat"><span>STAGE</span><b>${E(t.stage)}</b></div><div class="report-stat"><span>SCORE</span><b>${t.score}/100</b></div><div class="report-stat"><span>COMPETITION</span><b>${E(t.competition)}</b></div><div class="report-stat"><span>WINDOW</span><b>${E(t.window)}</b></div></div><div class="report-section"><h3>WHY IT'S MOVING</h3><p>${E(t.why)}</p></div><div class="report-section"><h3>WHAT TO DO</h3><p>${E(t.action)}</p></div><div class="report-section"><h3>3 CONTENT HOOKS</h3><ul>${t.hooks.map(h=>`<li>${E(h)}</li>`).join("")}</ul></div><div class="report-section"><h3>KEYWORDS</h3><p>${t.keywords.map(E).join(" • ")}</p></div><div class="report-section"><h3>MONETIZATION</h3><p>${E(t.monetization)} potential • ${t.creators.map(E).join(" • ")}</p></div><div class="report-actions"><button onclick='useTrend(${JSON.stringify(t.title)})'>✦ Create content</button></div>`;$("#modal").classList.remove("hidden")}
window.useTrend=t=>{$("#modal").classList.add("hidden");$("#trendInput").value=t;$("#generator").scrollIntoView({behavior:"smooth"})};$("#close").onclick=$("#x").onclick=()=>$("#modal").classList.add("hidden");$("#find").onclick=today;$("#refresh").onclick=load;$("#geo").onchange=e=>{S.geo=e.target.value;load()};$("#search").oninput=e=>{S.search=e.target.value.toLowerCase();render()};$$(".chip").forEach(b=>b.onclick=()=>{$$(".chip").forEach(x=>x.classList.remove("active"));b.classList.add("active");S.stage=b.dataset.stage||"all";S.comp=b.dataset.comp||"";render()});$$("[data-go]").forEach(b=>b.onclick=()=>{$("#"+b.dataset.go).scrollIntoView({behavior:"smooth"});$("#sidebar").classList.remove("open")});$("#menu").onclick=()=>$("#sidebar").classList.toggle("open");$("#generate").onclick=async()=>{let t=$("#trendInput").value.trim();if(!t)return;let r=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({trend:t,platform:$("#genPlatform").value})}),d=await r.json();$("#output").classList.remove("hidden");$("#output").innerHTML=[["Hook",d.hook],["Concept",d.concept],["Script",d.script],["Title",d.title],["Caption",d.caption],["Keywords",d.keywords],["Hashtags",d.hashtags],["Thumbnail",d.thumbnail]].map(([k,v])=>`<div><b>${k}</b><p>${E(v)}</p></div>`).join("")};load();